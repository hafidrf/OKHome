<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hafidrf.awesomeappassessment">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.hafidrf.awesomeappassessment.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AwesomeApp">
        <activity android:name="com.hafidrf.awesomeappassessment.ui.detail.ImageDetailActivity" />
        <activity
            android:theme="@style/Theme.AwesomeApp.NoActionBar"
            android:name="com.hafidrf.awesomeappassessment.ui.main.MainActivity" />
        <activity
            android:name="com.hafidrf.awesomeappassessment.ui.splash.SplashActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.AwesomeApp.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>